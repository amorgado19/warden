# Rust build output
/target

# Local, writable OVMF/AAVMF firmware variables (copied per-checkout from edk2)
/OVMF_VARS.local.fd
OVMF_VARS*.fd
/AAVMF_VARS.local.fd
AAVMF_VARS*.fd

# QEMU ESP staging directories (x86_64 + aarch64) — build artifacts + locally
# generated test configs. Tracked fixtures live under fixtures/, copied by xtask.
/esp/
/esp-a64/

# Machine-local Claude Code settings + stray temp files
.claude/settings.local.json
.claude/*.tmp.*

# Editor / OS cruft
*.swp
*~
.DS_Store

# NOTE: Cargo.lock is intentionally committed (GC-04) — do not ignore it.

# P2 test assets (downloaded kernel, built initramfs) — do not commit
/test-assets/
CLAUDE.md
TASKS.md
docs/
